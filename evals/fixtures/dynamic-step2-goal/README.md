# Dynamic Step 2 Goal Fixture

Checks that Goal compilation for Step 2 includes project-specific active phase scope, deferred roadmap cards, and strict validation checkpoints instead of a static handoff only.
