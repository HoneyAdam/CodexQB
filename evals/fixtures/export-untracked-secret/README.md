# Export Untracked Secret Fixture

Checks that sanitized export rejects untracked secret-like content when `--include-untracked` is requested. This fixture intentionally describes the case without storing a real secret.
